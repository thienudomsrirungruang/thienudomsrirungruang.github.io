const warships = `A text-based CLI multiplayer online battleships clone. I was challenged to use only a default Python installation on Windows, running on a normal terminal, with no additional packages.

This was my first attempt combining TCP and client-server architecture with Python, and I developed a box-based "graphics" drawing system to stay inside the challenge constraints, while having a GUI-like feel to the game.`;

export default warships;