const impostor = `A chatbot that uses AI to emulate my texting habits. Uses the GPT-2 language model trained on 20,000 of my chat messages. Can be interacted with using the messaging app Discord (the bot is not currently hosted, but you can train and host the bot yourself).`;

export default impostor;